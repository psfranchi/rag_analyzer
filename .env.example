# App config (.env) — same on macOS and Linux
#
# Install Postgres + Ollama per docs/SETUP.md (OS-specific steps).
# Then copy this file:  cp .env.example .env

DATABASE_URL=postgresql://rag:rag@localhost:5432/rag_analyzer
OLLAMA_URL=http://localhost:11434
ANALYSIS_MODEL=qwen3:8b
EMBEDDING_MODEL=nomic-embed-text
